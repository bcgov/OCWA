apiVersion: v1
kind: Secret
metadata:
  name: {{ template "formio.fullname" . }}-secrets
  labels:
    app: {{ template "formio.name" . }}
    chart: {{ template "formio.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
data:
  jwtSecret: {{ .Values.global.jwtSecret | b64enc }}
  dbPassword: {{ .Values.database.password | b64enc }}
  