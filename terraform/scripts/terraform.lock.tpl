
images = {
    owner = "${owner}"
    request_api = "${requestApiDigest}"
    policy_api = "${policyApiDigest}"}
    validate_api = "${validateApiDigest}"}
    forum_api = "${forumApiDigest}"}
    frontend = "${frontendDigest}"}
}
